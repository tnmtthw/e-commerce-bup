import React from "react";
import StatusTab from "@/app/admin/order/statusTab";
import HeaderCard from "@/components/admin/headerCard";

const OrderPage = () => {
  const btnName = "";
  const url = "";
  const className = "";
  return (
    <div>
      <HeaderCard className={className} btnName={btnName} url={url} />
      <StatusTab />
    </div>
  );
};

export default OrderPage;
